{
  "name": "al-bushra-community-backend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "deploy": "wrangler d1 execute DB --remote --file=./schema.sql --yes && wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "cloudflare": {
    "bindings": {
      "DB": {
        "description": "Shared AL-BUSHRA reader comments and good-news submissions database"
      }
    }
  }
}
